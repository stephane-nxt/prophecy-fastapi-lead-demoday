# Prophecy FastAPI

## Requirements (Docker)

- Docker
- Docker Compose

## Run with Docker


```bash
docker compose up -d
```

You can access it on : http://localhost:8000



Stop everything:

```bash
docker compose down
```
